{% import 'ui/components/game/console-badge.twig' as consolebadge %}

{% if ItemList.count > 0 %}
    <table class="table data-sortable">
        <thead>
        <tr>
            <th class="text-center">Link id</th>
            <th class="text-left">Title</th>
            <th class="text-center">Release date (EU)</th>
            {% if DataSource.isNintendoCoUk() %}
                <th class="text-center">Console</th>
                <th class="text-center">Price (standard)</th>
                <th class="text-center">Nintendo Url</th>
            {% endif %}
            {# Never wrap the actions: the title column absorbs the slack instead, so a
               long title wraps on its own row rather than breaking every row's links #}
            <th class="text-center" style="white-space: nowrap;">Options</th>
        </tr>
        </thead>
        <tbody>
        {% for item in ItemList %}
            <tr>
                <td class="text-center">
                    <a href="{{ route('staff.data-sources.detail', {'sourceId': item.source_id, 'linkId': item.link_id}) }}">{{ item.link_id }}</a>
                </td>
                <td class="text-left">
                    {{ item.title }}
                </td>
                <td class="text-center">
                    {{ item.release_date_eu }}
                </td>
                {% if DataSource.isNintendoCoUk() %}
                    <td class="text-center">
                        {{ consolebadge.standard(item) }}
                    </td>
                    <td class="text-center">
                        {{ item.price_standard }}
                    </td>
                    <td class="text-center">
                        <a href="{{ eshop_url('eu', item.url) }}" target="_blank">View</a>
                    </td>
                {% endif %}
                <td class="text-center" style="white-space: nowrap;">
                    {% if DataSource.isNintendoCoUk() %}
                        <a href="{{ route('staff.data-sources.nintendo-co-uk.add-game', {'itemId': item.id}) }}">Add game</a> :
                    {% endif %}
                    {% if ListRef == 'ignored' %}
                        <a href="javascript:void(0);" id="lnk-remove-ignore-{{ item.id }}" class="lnk-remove-ignore">Un-ignore</a>
                    {% else %}
                        <a href="javascript:void(0);" id="lnk-add-ignore-{{ item.id }}" class="lnk-add-ignore">Ignore</a>
                    {% endif %}
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% else %}
    <p>No items found!</p>
{% endif %}
