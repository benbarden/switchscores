{% extends 'theme/wos/staff/clean-narrow.twig' %}

{% set crumbNav = [
    {'url': route('staff.data-sources.dashboard'), 'text': 'Data sources'},
    {'text': PageTitle}
] %}

{% block page_inner %}

<p>
    This tool does a few things:
</p>
<ol>
    <li>imports data from Wikipedia and stores its raw data</li>
    <li>parses this data into a structure that's usable for querying and updating</li>
    <li>attempts to link the imported data to games in the Switch Scores database</li>
</ol>
<p>
    This process is scheduled to run overnight. <strong>As it loads HTML from Wikipedia directly,
    it's important not to run it too often</strong>.
</p>

<form class="form-horizontal" role="form" method="post" action="{{ route('staff.data-sources.tools.wikipedia.importParseLink') }}">
    {{ csrf_field() }}
    <input type="submit" value="Start process" class="btn btn-primary">
</form>

{% endblock page_inner %}
