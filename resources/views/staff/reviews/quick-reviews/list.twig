{% import 'ui/components/links/game.twig' as gamelink %}

{% extends 'theme/staff-b5/layout-default.twig' %}

{% block page_inner %}

<div class="row">
    <div class="col-12 col-sm-12">
        <div class="float-end">
            <label for="admin-nav-status-filter">Filter by status:</label>
            <select id="admin-nav-status-filter" name="admin-nav-status-filter" class="form-control">
                <option value="">All</option>
                {% for item in QuickReviewStatusList %}
                    <option value="{{ item.id }}">{{ item.title }}</option>
                {% endfor %}
            </select>
        </div>
    </div>
</div>

<hr>
{% if QuickReviewList.count > 0 %}

    <table class="table data-sortable">
        <thead>
        <tr>
            <th class="text-end">#</th>
            <th class="text-start">Details</th>
            <th class="text-center">Date</th>
            <th class="text-center">Score</th>
            <th class="text-center">Status</th>
            <th class="text-center">Options</th>
        </tr>
        </thead>
        <tbody>
        {% for item in QuickReviewList %}
            <tr>
                <td class="text-end">
                    {{ item.id }}
                </td>
                <td class="text-start" style="max-width: 400px;">
                    {{ gamelink.title(item.game, 'staff') }}
                    <br>
                    <small>
                        Submitted by <a href="{{ route('owner.user.view', {'userId': item.user.id}) }}">{{ item.user.display_name }}</a>
                    </small>
                    <hr class="my-1">
                    <small><strong>Review body:</strong> {{ item.review_body }}</small>
                </td>
                <td class="text-center">
                    {{ item.created_at }}
                </td>
                <td class="text-center">
                    {{ item.review_score }}
                </td>
                <td class="text-center">
                    {{ item.getStatusDesc() }}
                </td>
                <td class="text-center">
                    {% if item.item_status != constant('App\\Models\\QuickReview::STATUS_ACTIVE') %}
                        <form method="post" action="{{ route('staff.reviews.quick-reviews.approve', {'reviewId': item.id}) }}" class="d-inline">
                            {{ csrf_field() }}
                            <button type="submit" class="btn btn-success btn-sm">Approve</button>
                        </form>
                    {% endif %}
                    {% if item.item_status != constant('App\\Models\\QuickReview::STATUS_REJECTED') %}
                        <form method="post" action="{{ route('staff.reviews.quick-reviews.reject', {'reviewId': item.id}) }}" class="d-inline">
                            {{ csrf_field() }}
                            <button type="submit" class="btn btn-danger btn-sm">Reject</button>
                        </form>
                    {% endif %}
                    <a href="{{ route('staff.reviews.quick-reviews.delete', {'reviewId': item.id}) }}" class="btn btn-outline-secondary btn-sm">Delete</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% else %}
    <p>No reviews found!</p>
{% endif %}

{% include 'ui/layouts/assets/table-sorting-b5.twig' %}
<script>
    $('#admin-nav-status-filter').on('change', function() {
        selectedStatusFilter = $(this).val();
        top.location.href = '{{ route('staff.reviews.quick-reviews.list')}}?filterStatus=' + selectedStatusFilter;
    });
    $(document).ready(function() {
        var activeStatusId = '{{ FilterStatus }}';
        $('#admin-nav-status-filter').val(activeStatusId);
        $('.data-sortable').DataTable({
            "order": [{{ jsInitialSort|raw }}],
            "pageLength": 25,
            "columns": [
                null,
                null,
                null,
                null,
                { "orderable": false },
                { "orderable": false }
            ]
        });
    });
</script>

{% endblock page_inner %}
