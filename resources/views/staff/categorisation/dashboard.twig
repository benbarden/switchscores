{% extends 'theme/staff-b5/layout-default.twig' %}

{% block page_inner %}

    <div class="row">

        <div class="col-md-4">

            {% include 'modules/tables/slick-header-title.twig' with {'titleText': 'Action lists'} %}

            <table class="table">
                <tbody>
                <tr>
                    <td>
                        <a href="{{ route('staff.games.list.no-category') }}">
                            No category
                        </a>
                    </td>
                    <td class="text-end">
                        <span class="h4">{{ NoCategoryCount }}</span>
                    </td>
                </tr>
                </tbody>
            </table>

        </div>

        <div class="col-md-4">

            {% include 'modules/tables/slick-header-title.twig' with {'titleText': 'Navigation'} %}
            <table class="table">
                <tbody>
                <tr>
                    <td>
                        <a href="{{ route('staff.categorisation.category.list') }}">Categories</a>
                    </td>
                </tr>
                <tr>
                    <td>
                        <a href="{{ route('staff.categorisation.tag.list') }}">Tags</a>
                    </td>
                </tr>
                <tr>
                    <td>
                        <a href="{{ route('staff.categorisation.game-series.list') }}">Series</a>
                    </td>
                </tr>
                <tr>
                    <td>
                        <a href="{{ route('staff.categorisation.game-collection.list') }}">Collections</a>
                    </td>
                </tr>
                </tbody>
            </table>

        </div>

        <div class="col-md-4">

            {% include 'modules/tables/slick-header-title.twig' with {'titleText': 'Series matches'} %}
            {% if GameSeriesMatchList %}
                <table class="table">
                    <thead>
                    <tr>
                        <th>Series</th>
                        <th class="text-end">Potential matches</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for item in GameSeriesMatchList %}
                        <tr>
                            <td>{{ item.name }}</td>
                            <td class="text-end">
                                <span class="h4">
                                    <a href="{{ route('staff.categorisation.title-match.series', {'gameSeries': item.series}) }}">
                                        {{ item.gameCount }}
                                    </a>
                                </span>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            {% else %}
                <p>No outstanding matches to review.</p>
            {% endif %}

        </div>

    </div>

{% endblock page_inner %}
