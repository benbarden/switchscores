<h2>More lists</h2>
<ul>
    {% if thisPage != 'released' %}
        <li><a href="{{ route('games.list.released') }}">All released Switch titles</a></li>
    {% endif %}
    {% if thisPage != 'upcoming' %}
        <li><a href="{{ route('games.list.upcoming') }}">Upcoming Switch releases</a></li>
    {% endif %}
    {% if thisPage != 'genresLanding' %}
        <li><a href="{{ route('games.genres.landing') }}">Released Switch games by genre</a></li>
    {% endif %}
</ul>