<nav class="navbar navbar-default navbar-static-top">
    <div class="container">
        <div class="navbar-header">

            <!-- Collapsed Hamburger -->
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#app-navbar-collapse">
                <span class="sr-only">Toggle Navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>

            <!-- Branding Image -->
            <a class="navbar-brand" href="{{ url('/') }}" style="padding: 8px 10px;" title="World of Switch">
                <img src="/img/logo.png" alt="World of Switch" class="img-responsive" style="max-height: 30px; max-width: 30px;">
            </a>
        </div>

        <div class="collapse navbar-collapse" id="app-navbar-collapse">
            <!-- Left Side Of Navbar -->
            <ul class="nav navbar-nav">
                <li><a href="{{ route('games.landing') }}">Browse games</a></li>
                <li><a href="{{ route('games.recentReleases') }}">Recent</a></li>
                <li><a href="{{ route('games.upcomingReleases') }}">Upcoming</a></li>
                <li><a href="{{ route('games.browse.byTitle.landing') }}">Games A-Z</a></li>
                <li><a href="{{ route('games.browse.byDate.landing') }}">Calendar</a></li>
                <li><a href="{{ route('topRated.landing') }}">Top Rated</a></li>
                <li><a href="{{ route('reviews.landing') }}">Reviews</a></li>
                <li><a href="{{ route('news.landing') }}">News</a></li>
            </ul>

            <!-- Right Side Of Navbar -->
            <ul class="nav navbar-nav navbar-right">
                <!-- Authentication Links -->
                {% if auth_guest() %}
                    <li><a href="{{ route('login') }}">Login</a></li>
                    <li><a href="{{ route('register') }}">Register</a></li>
                {% else %}
                    <li><a href="{{ route('user.index') }}">Members</a></li>
                    <li>
                        <a href="{{ route('logout') }}"
                           onclick="
                             event.preventDefault();
                             document.getElementById('logout-form').submit();">
                            Logout
                        </a>

                        <form id="logout-form" action="{{ route('logout') }}" method="POST" style="display: none;">
                            {{ csrf_field() }}
                        </form>
                    </li>
                    {% if auth_user().isStaff() %}
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
                                Staff <span class="caret"></span>
                            </a>
                            <ul class="dropdown-menu" role="menu">
                                <li><a href="{{ route('staff.index') }}">Staff index</a></li>
                            </ul>
                        </li>
                    {% endif %}
                {% endif %}
            </ul>

        </div>
    </div>
</nav>
