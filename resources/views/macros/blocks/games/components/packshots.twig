{% macro header(item) %}
    {% set boxartUrl = ImageHelper.imageHeaderUrl(item) %}
    <a href="{{ route('game.show', {'id': item.id, 'linkTitle': item.link_title}) }}">
        {% if boxartUrl %}
            <img src="{{ boxartUrl }}" class="img-responsive" style="border: 0; width: 100%;" alt="{{ item.title }}">
        {% else %}
            <img src="/img/placeholder/no-image-found-wide.png" class="img-responsive" style="border: 0; width: 100%;" alt="{{ item.title }}">
        {% endif %}
    </a>
{% endmacro %}

{% macro square(item) %}
    {% set boxartUrl = ImageHelper.imageSquareUrl(item) %}
    <a href="{{ route('game.show', {'id': item.id, 'linkTitle': item.link_title}) }}">
        {% if boxartUrl %}
            <img src="{{ boxartUrl }}" class="img-responsive" style="border: 0; width: 100%;" alt="{{ item.title }}">
        {% else %}
            <img src="/img/placeholder/no-image-found-square.png" class="img-responsive" style="border: 0; width: 100%;" alt="{{ item.title }}">
        {% endif %}
    </a>
{% endmacro %}

{% macro squareReleaseHub(item) %}
    {% set boxartUrl = ImageHelper.imageSquareUrl(item) %}
    <a href="{{ route('game.show', {'id': item.id, 'linkTitle': item.link_title}) }}">
        {% if boxartUrl %}
            <img src="{{ boxartUrl }}" class="img-thumbnail me-2 align-middle" style="border: 0; width: 75px; height: auto;" alt="{{ item.title }}">
        {% else %}
            <img src="/img/placeholder/no-image-found-square.png" class="img-thumbnail me-2 align-middle" style="border: 0; width: 50px; height: auto;" alt="{{ item.title }}">
        {% endif %}
    </a>
{% endmacro %}